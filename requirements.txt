# AegisFlow Backend Dependencies
# Python 3.11+
# Install with: pip install -r requirements.txt

# ── Web Framework ────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0          # ASGI server with websocket support

# ── LangGraph (Orchestration Plane) ─────────────────────────────────────────
langgraph>=0.2.28                   # StateGraph, MemorySaver, interrupt(), Command
langgraph-checkpoint>=1.0.6         # Checkpoint base classes

# ── Data Validation (Pydantic v2) ────────────────────────────────────────────
pydantic>=2.7.0                     # v2 required — field_validator, model_validator
pydantic-settings>=2.3.0            # For future environment variable config

# ── HTTP Client (Integration Plane) ─────────────────────────────────────────
httpx>=0.27.0                       # Async HTTP client for Ollama + MCP Gateway calls

# ── Async Support ─────────────────────────────────────────────────────────────
anyio>=4.4.0                        # Async primitives — used by FastAPI/LangGraph
# asyncio — part of Python stdlib, no install needed

# ── Logging & Observability ──────────────────────────────────────────────────
python-json-logger>=2.0.7           # Structured JSON log formatting for production

# ── Security ─────────────────────────────────────────────────────────────────
python-multipart>=0.0.9             # Multipart form support for FastAPI file uploads
cryptography>=42.0.0                # Ed25519 signatures for document attestation (future)

# ── Development / Testing ────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx                               # Also used for TestClient in pytest
